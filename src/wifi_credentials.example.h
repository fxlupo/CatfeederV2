#pragma once

// Copy this file to src/wifi_credentials.h for local builds.
// The real credentials file is intentionally ignored by Git.
#define WIFI_DEFAULT_SSID "your-ssid"
#define WIFI_DEFAULT_PASS "your-password"
